# 0.7.0 deferred-item implementation status

**Updated:** 2026-08-25  
**Target release:** 0.7.0  
**Related:** [0.6.0 implementation status](./0.6.0-implementation-status.md), [deployment research](./0.7.0-deployment-routes.md), [Socket remediation research](./0.7.0-socket-dependency-remediation.md)

This document is the current disposition of deferred work. A deferred item is
closed only when code, documentation, and verification evidence exist. A
provider recipe is not called certified merely because its runtime resembles a
local Node process.

## Closed by existing 0.6.0 work

| Deferred item | Evidence |
| --- | --- |
| Dynamic framework copy | `handlerFrom` with typed multi-argument and promised-parameter tests. |
| Next metadata-file shape | Packed Next static export exercises `opengraph-image`, `size`, `contentType`, and metadata output. |
| Astro static endpoint | Packed Astro static build and image/page verification. |
| React Router resource route | Packed framework-mode typegen, typecheck, build, server, page, and dynamic image fetch. |
| Express response bridge | Packed Express 5 HTTP smoke verifies bytes, media type, cache header, and dimensions. |
| Runtime boundary documentation | README and `docs/deployment.md` distinguish Node, static, and edge/Wasm routes. |
| Social compatibility profiles | Local target profiles and format/URL guardrails shipped in 0.6.0. |
| Native renderer dimension invariant | Resvg dimension-changing options are rejected and actual output dimensions are checked. |
| Font/project asset loading | Fontsource, project-file, package, and runtime-byte loaders are shipped and packed-tested. |

## Closed by this 0.7.0 work

| Deferred item | Evidence |
| --- | --- |
| Deployment claim status | `deployment-evidence.json` records every documented route, runtime, status, official evidence link, and the edge non-support reason. |
| Release deployment evidence policy | `RELEASING.md` and `verify-deployment-evidence.mjs` require evidence before a route can be marked certified. |
| Socket disposition workflow | `socket-dispositions.json` and `verify-socket-dispositions.mjs` define required alert fields and prevent an incomplete report from being called clean. |
| Dependency inventory | `dependency-inventory.mjs` and `verify-dependency-inventory.mjs` classify lockfile packages, native candidates, install scripts, and remote resolutions. CI uploads the full JSON inventory. |
| Supply-chain gate integration | `npm run check` now runs dependency, deployment, and Socket disposition checks. |

## Still blocked or intentionally separate

| Item | Reason and next action |
| --- | --- |
| Exact Socket alert remediation | The linked Socket page did not expose alert rows. Import the Socket export/API/report, populate `socket-dispositions.json`, then upgrade/replace/remove/isolate each high/critical package and rerun the package gate. |
| SvelteKit certification | The release-tested latest stable dependency graph has an upstream Cookie advisory. Revisit when the adapter's dependency graph is fixed or an isolated fixture can be audited cleanly. |
| Next 15 certification | The declared support line remains Next 16.3.2–16.x because the audited Next 15 graph still fails the repository's high-severity audit policy. |
| Next dynamic-server and Astro SSR certification | Requires adding the corresponding server adapters and maintaining fixture-specific production runners. The current release only claims static builds plus dynamic React Router/Express evidence. |
| Remote `verify-url` | Requires a security-reviewed fetcher with redirect DNS/IP re-resolution, private/link-local rejection, scheme/port controls, byte/time limits, and credential isolation. It is not safe to add as a thin `fetch(url)` command. |
| Page-level metadata/HTML serializer | Remains a separate API design because framework metadata unions, tag ordering, canonical URLs, and escaping need a stable contract and dedicated tests. |
| Square/landscape plate variants | Requires layout-aware reflow rather than automatic cropping. Keep as renderer design work. |
| Edge/Wasm renderer | Requires a real renderer, font, asset, and output-compatibility fixture. The native Node API is not silently changed. |
| X player/app cards, OGP video/audio, Schema.org, Adaptive Cards, app unfurls | Separate contracts with different required fields and hosting/security semantics. |
| Bun/Deno certification | Requires dedicated runtime matrices; Web API compatibility alone is insufficient evidence. |
| Streaming output | Requires measured encoder/provider behavior and changes the current buffered-response contract. |

## Release rule

The 0.7.0 release may claim documented recipes for the routes listed in
`deployment-evidence.json`, but only a route with complete artifact, build,
output, image, and metadata evidence may be marked `certified`. Socket status
must remain `awaiting-alert-export` until the individual alerts are available;
`npm audit` passing does not change that status.
