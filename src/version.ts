/**
 * The CLI's release identifier. Keep this in sync with package.json; the CLI
 * tests deliberately compare the two so a release cannot quietly drift.
 */
export const METAPLATE_VERSION = "0.6.0";
